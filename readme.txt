to run the server ./docker_start or sudo ./docker_start if that doesnt work
this assumes docker is already installed on the system. This does also rebuild the docker container to if there where any changes.
may need to to give the bash script premission to run as a program just right click the file the and select check box next execute allowning the script to run as a program